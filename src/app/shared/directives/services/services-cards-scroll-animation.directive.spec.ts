import { ServicesCardsScrollAnimationDirective } from './services-cards-scroll-animation.directive';

describe('ServicesCardsScrollAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ServicesCardsScrollAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
